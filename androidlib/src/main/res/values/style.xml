<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SwipeBackLayout">
        <item name="edge_size">50dp</item>
        <item name="shadow_left">@mipmap/shadow_left</item>
        <item name="shadow_right">@mipmap/shadow_right</item>
        <item name="shadow_bottom">@mipmap/shadow_bottom</item>
    </style>

    <!-- 通用标题栏的属性 -->
    <style name="title_bar_view_layout">
        <item name="android:alpha">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/normal_title_bar_height</item>
        <item name="android:background">@color/white</item>
    </style>

    <!-- 通用分割线 -->
    <!--横线-->
    <style name="divider_hz">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_0.5</item>
        <item name="android:background">@color/color_common_bg</item>
    </style>


    <!-- 通用Activity进入退出动画 -->
    <style name="activityAnimation" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/slide_right_in</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_right_in</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_right_out</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_right_out</item>
    </style>

    <!-- 对话框样式 -->
    <style name="commonDialog" parent="@android:style/Theme.Dialog">
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 透明背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有边框 -->
        <!--<item name="android:windowFrame">@null</item>-->
        <!--是否在悬浮Activity之上  -->
        <!--<item name="android:windowIsFloating">true</item>-->
        <!--阴影  -->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
    </style>


    <!-- LoadingDialog 对话框使用的主题 -->
    <style name="commonLoadingDialog" parent="@android:style/Theme.Dialog">
        <!-- 透明背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否显示在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
    </style>

    <!-- loading对话框样式（白色透明背景） -->
    <style name="loadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowBackground">@color/transparent</item><!--背景透明-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:backgroundDimEnabled">false</item><!--模糊-->
    </style>

    <!-- dialog 弹窗动画 -->
    <style name="commonDialogAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <!-- dialog 弹窗动画 -->
    <style name="redPackageDialogAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_red_package_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_red_package_exit_anim</item>
    </style>

    <!-- 默认使用,所以必须拷贝此 style  -->
    <style name="JDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 决定背景透明度 -->
        <item name="android:backgroundDimAmount">0.3</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画,这样所有使用此 style 都有这个动画, 可以单独指定的 -->
        <!--<item name="android:windowAnimationStyle">@style/JDialogAnimation</item>-->
    </style>

    <!-- 默认使用,所以必须拷贝此 style -->
    <!-- 自定义动画,可以使用系统的,即把parent= 后面的直接 "android:windowAnimationStyle">   -->
    <style name="JDialogAnimation" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/jdialog_enter</item>
        <item name="android:windowExitAnimation">@anim/jdialog_exit</item>
    </style>
    <!-- dialog底部弹出菜单动画 -->
    <style name="bottom_menu_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/bottom_menu_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_menu_exit</item>
    </style>

    <!-- 升级时使用的类似效果 -->
    <style name="updateAnimation" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/update_scale_fade_in</item>
        <item name="android:windowExitAnimation">@anim/update_scale_fade_out</item>
    </style>

</resources>